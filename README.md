# STARS System Bot website

Standalone bilingual command documentation built with plain HTML, CSS, and JavaScript.

Open `index.html` directly, or serve this folder locally:

```powershell
node website/server.js
```

The command catalog lives in `commands.js`. UI translations and interaction logic live in `app.js`. No build step or external runtime dependency is required.
